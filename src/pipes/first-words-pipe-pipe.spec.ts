import { FirstWordsPipePipe } from './first-words-pipe-pipe';

describe('FirstWordsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FirstWordsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
